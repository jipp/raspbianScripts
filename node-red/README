installation:
bash <(curl -sL https://raw.githubusercontent.com/node-red/raspbian-deb-package/master/resources/update-nodejs-and-nodered)

npm -g install node-red-dashboard
apt install sqlite3
npm -g install node-red-node-sqlite

configuration:

enable/start:

backup:
